---
import BaseLayout from "../layouts/BaseLayout.astro";

const title = "Home";
const description = "Mathilde's digital garden homepage";
const permalink = Astro.site!.href;
---

<BaseLayout title={title} description={description} permalink={permalink}>
  <div class="home-container">
    <div class="home-copy">
      <div class="presentation">
        <img src="/assets/zoomed-smiling-avatar.svg" alt="Mathilde Sauvage" />
        <div class="text">
          <h1>Hi, I'm Mathilde!</h1>
        </div>
      </div>
      <div class="section">
        <p>
          🌶️🌱🫐
          <br />
          Welcome to my digital garden!<br />A place where I collect concise
          notes and experiments.
          <br />
          🫜🍄‍🟫🫛
        </p>
      </div>
      <div class="section">
        <p>I built this site for two purposes:</p>
        <ul>
          <li>to access my programming notes from anywhere</li>
          <li>to experiment with CSS for fun</li>
        </ul>
        <p>I write concise articles that get straight to the point.</p>
        <p>
          As I am a full-stack software engineer, you will find content about
          diverse subjects, from pure CSS to micro-services.
        </p>
      </div>
    </div>
  </div>

  <style>
    .home-container {
      position: relative;
      margin: 2em 0;
      min-height: 400px;
    }

    .home-copy {
      flex: 1;
      padding: 0 24em;
    }

    .home-copy p {
      font-size: 1.4em;
    }

    /* By default, center all sections of text */
    .home-copy > .section {
      text-align: center;
    }

    /* Set all sections but the first one to left-align 
    -> this keeps text-align center for the first section only */
    .home-copy > .section ~ .section {
      text-align: left;
    }

    .home-copy .section {
      margin-bottom: 2em;
    }

    .presentation:hover {
      cursor:
        url('data:image/svg+xml;utf8,<svg width="40" height="48" viewBox= "0 0 100 100" xmlns="http://www.w3.org/2000/svg" style="fill:black;font-size:48px;"><text y="50%">🐝</text></svg>'),
        auto;
    }

    .presentation {
      display: flex;
      align-items: stretch;
      margin-top: 2em;
      margin-bottom: 4em;
      /* needed for the ::before element */
      position: relative;
    }

    .presentation::before {
      content: "";
      position: absolute;
      transform: translateY(0.5em);
      left: 30%;
      bottom: -2em;
      width: 40%;
      height: 3px;
      background: var(--text-secondary);
    }

    .presentation img {
      max-width: 33%;
      border-radius: 80px 0 0 80px;
    }

    .presentation .text {
      border-right: solid 2px var(--text-secondary);
      border-radius: 0 80px 80px 0;
      display: flex;
      align-items: center;
    }

    .presentation h1 {
      font-weight: 700;
      margin: 0;
      padding-right: 1em;
    }

    @media (max-width: 1200px) {
      p {
        font-size: 1.2em;
      }
    }

    @media (max-width: 800px) {
      .home-container {
        flex-direction: column;
      }

      .home-copy {
        flex: 0;
        padding: 0 1em 2em 1em;
        text-align: center;
      }
    }
  </style>
</BaseLayout>
